version: "3.9"
services:
  backend:
    build: ../backend
    env_file: ../.env
    ports:
      - "8000:8000"
    command: bash -c "python -m app.seed && uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2"
  frontend:
    build: ../frontend
    ports:
      - "5173:5173"
